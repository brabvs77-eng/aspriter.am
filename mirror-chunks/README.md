# Download chunks (18 parts)

Generated by `python3 scripts/split-chunks.py --parts 18`.

| Chunk | URLs | Contents |
|-------|------|----------|
| 01 | 128 | All site pages (84) + first 44 products |
| 02–10 | 44 each | Product pages |
| 11–18 | 43 each | Product pages |

**Total:** 868 URLs (84 site + 784 products)

## GitHub Actions

Run **Actions → Restore Wayback (18 chunks) → Run workflow**.

- All 18 chunks: leave `chunk` empty (~15 min, 3 parallel)
- Single chunk: set `chunk` to `1`…`18` (re-download one part)

Chunks are saved to `mirror-chunks/01/` … `mirror-chunks/18/` and committed to git.

## Cloudflare Pages

After all chunks are in git, CF build runs `assemble-and-build.sh` (fast, &lt;5 min).
